services:
  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    ports:
      - '3000:3000'
    volumes:
      - ${DOCKER_PATH}/grafana:/var/lib/grafana
    environment:
      - GF_PLUGINS_PREINSTALL=grafana-cloudwatch-datasource
    restart: unless-stopped